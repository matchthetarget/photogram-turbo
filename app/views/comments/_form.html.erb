<li id="photo_<%= comment.photo_id %>_comments_form" class="list-group-item">
  <%= turbo_frame_tag dom_id(comment) do %>
    <%= form_with(model: comment) do |form| %>
      <% if comment.errors.any? %>
        <div id="error_explanation">
          <ul class="list-unstyled">
            <% comment.errors.each do |error| %>
              <li>
                <div class="alert alert-danger" role="alert">
                  <%= error.full_message %>
                </div>
              </li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form.hidden_field :photo_id %>

      <div class="form-group">
        <%= form.label :body, class: "visually-hidden" %>
        <%= form.text_area :body, class: "form-control" %>
      </div>

      <div class="actions d-flex justify-content-between">
        <%= form.submit class: "btn btn-outline-secondary btn-block" %>
        <% if comment.persisted? %>
          <%= link_to "Cancel", comment.photo %>
        <% end %>
      </div>
    <% end %>
  <% end %>
</li>
